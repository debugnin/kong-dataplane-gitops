apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kong-dataplane-nab
  namespace: argocd
spec:
  project: kong-dataplane
  source:
    repoURL: https://charts.konghq.com
    chart: kong
    targetRevision: 2.41.1
    helm:
      releaseName: kong-nab-dataplane
      valueFiles:
        - $values/argocd/applications/customers/base-values.yaml
        - $values/argocd/applications/customers/nab/values.yaml
      values: |
        fullnameOverride: kong-nab-dataplane-kong
  sources:
    - repoURL: https://charts.konghq.com
      chart: kong
      targetRevision: 2.41.1
      helm:
        releaseName: kong-nab-dataplane
        valueFiles:
          - $values/argocd/applications/customers/base-values.yaml
          - $values/argocd/applications/customers/nab/values.yaml
        values: |
          fullnameOverride: kong-nab-dataplane-kong
    - repoURL: https://github.com/debugnin/kong-dataplane-gitops
      targetRevision: HEAD
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: kong-nab
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true